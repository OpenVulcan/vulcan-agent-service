# 任务目标

优化 `vulcan_codekite_skill.task` 的候选项文案，使其从“模糊意图”收敛为“明确工作指令”，更适合直接作为用户可选操作使用。

# 执行步骤

1. 检查当前 `task` 参数在 `skill.json` 中配置的 completion 文案。
2. 将模糊表达改为明确工作项，例如分析当前项目、总结项目特点、定位调用归属、准备整函数替换等。
3. 做最小必要校验，确认 `skill.json` 解析仍然通过。
4. 追加执行变更总结并归档计划文件。

# 技术选型

- 仅调整 `skill.json` 中的 completion 文案，不改动 Rust 逻辑。
- 候选项保持数量克制，并统一成明确动作句式。

# 验收标准

- `task` completion 文案变成明确工作指令。
- `skill.json` 解析通过。
- 计划文件完成执行总结并归档。

## 执行变更总结

### 1. 核心修复与调整概述

- 将 `vulcan_codekite_skill.task` 的 completion 候选项从“模糊意图”收敛为“明确工作项”。
- 现在每个候选项都可以直接作为当前任务使用，而不是还需要模型或用户再解释一层。

### 2. 📂文件变更清单

- 修改：`runtime/lua_skills/vulcan-codekit/skill.json`

### 3. 💻关键代码调整详情

- 将 6 条 completion 文案统一改成明确动作句式，覆盖：
  - 分析当前项目并总结结构与模块职责
  - 根据信号搜索并识别所属函数或类
  - 对已选文件做 AST 细查并总结符号与签名
  - 扫描 Markdown 文档并定位相关文件或标题
  - 为安全整函数/整方法替换做准备
  - 先建图再决定后续检查或委派的文件

### 4. ⚠️遗留问题与注意事项

- 本次只调整了 completion 文案，没有改动 completion 机制本身。
- `skill.json` 解析校验已通过。
