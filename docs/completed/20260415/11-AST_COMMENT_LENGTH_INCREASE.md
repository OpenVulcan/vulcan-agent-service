# 任务目标

将 `vulcan-codekit` 中 AST 备注摘要的默认长度上限提升到更易理解的范围，优先按 100 的默认值执行，并同步更新相关回归验证，避免英文备注被过早截断导致理解困难。

# 执行步骤

1. 定位 AST 备注摘要默认长度的配置常量与相关回归校验逻辑。
2. 将默认备注长度上限提升到 100，并同步调整与之耦合的验证规则。
3. 执行备注摘要回归验证，确认截断行为、UTF-8 安全性与省略号逻辑仍然正确。
4. 补充执行变更总结，并按规范归档到 `docs/completed/20260415/`。

# 技术选型

- 复用现有按 UTF-8 字节边界截断的实现，不重写摘要算法。
- 采用最小改动方式，仅提升默认上限并同步修改验证脚本中的限制判断。
- 保留已实现的 `...` 追加逻辑，确保更长摘要在截断时仍具备语义延续性。

# 验收标准

1. AST 备注摘要默认长度上限提升为 100。
2. 截断摘要仍保持 UTF-8 安全，且发生截断时继续追加 `...`。
3. 备注摘要回归脚本通过，验证规则与新的默认上限保持一致。
4. 计划文件在任务完成后补齐执行变更总结，并归档到 `docs/completed/20260415/`。

---

# 执行变更总结

## 1. 核心修复与调整概述

已将 `codekit-ast` 备注摘要的默认长度上限从 50 提升到 100，并同步更新了回归验证脚本。这样英文备注和中英混排备注在默认展示下会更完整，可读性明显提升。

## 2. 📂文件变更清单

修改：

- `runtime/lua_skills/vulcan-codekit/main.lua`
- `scripts/verify_vmcp_ast_comment_notes.py`
- `docs/plan/20260415-11-AST_COMMENT_LENGTH_INCREASE.md`

新增：

- 无

删除：

- 无

## 3. 💻关键代码调整详情

- 将 `MAX_COMMENT_SUMMARY_BYTES` 的默认值调整为 `100`。
- 保持现有 UTF-8 安全截断与 `...` 追加逻辑不变，使更长备注在超限时仍然具备连续语义。
- 将验证脚本中的摘要长度限制同步提升到 100，确保运行时行为与回归规则一致。

## 4. ⚠️遗留问题与注意事项

- 当前长度限制仍按 UTF-8 字节预算控制，而不是按“字符数”精确计数；这对英文最友好，对中文则大约相当于更少的可见字数。
- 本次验证使用 `python scripts/verify_vmcp_ast_comment_notes.py`，未额外执行其它工具链回归。
