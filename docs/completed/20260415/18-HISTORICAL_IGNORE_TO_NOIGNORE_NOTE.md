# 任务目标

为历史文档补充 `ignore -> noignore` 的迁移说明，避免后续检索旧归档或旧设计记录时，被已经失效的参数语义误导。

# 执行步骤

1. 定位仍然使用旧 `ignore` 参数语义的关键历史文档。
2. 在文档开头补充统一迁移备注，明确当前运行时已切换为 `noignore`。
3. 检查说明表述是否一致，避免出现新旧语义混杂。
4. 完成执行总结并归档计划文件。

# 技术选型

- 保留历史正文不改写，只在文档前部增加“迁移说明”，兼顾历史可追溯性与当前可用性。
- 只补充关键会被检索命中的历史文档，避免无意义批量改动。

# 验收标准

- 关键历史文档已补充 `ignore -> noignore` 迁移说明。
- 说明能明确指出当前默认行为与 `noignore=true` 的含义。
- 计划文件完成归档。

# 执行变更总结

## 1. 核心修复与调整概述

- 为两份仍会被检索命中的历史归档文档补充了 `ignore -> noignore` 迁移说明。
- 说明统一明确了当前默认行为仍启用忽略规则，只有显式传入 `noignore=true` 时才会关闭忽略规则。
- 本次保留历史正文不改写，仅通过前置备注降低旧文档对后续检索与推理的误导风险。

## 2. 📂文件变更清单

- 修改：`docs/completed/20260413/13-CODEVIEW_AST_IGNORE_RULES_AND_FILE_LIMIT.md`
- 修改：`docs/completed/20260415/05-MARKDOWN_MENU_TOOL.md`
- 修改：`docs/plan/20260415-18-HISTORICAL_IGNORE_TO_NOIGNORE_NOTE.md`

## 3. 💻关键代码调整详情

- 本次未调整运行时代码。
- 在旧归档文档开头新增统一迁移备注，明确旧 `ignore` 语义已废弃，并给出当前 `noignore` 的使用方式。

## 4. ⚠️遗留问题与注意事项

- 当前只补充了最容易被后续检索命中的关键历史文档，其他更早期或低频文档若仍含旧语义，后续可按实际检索命中情况继续补充。
