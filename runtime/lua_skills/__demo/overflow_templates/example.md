# Static Overflow Template Example: {{topic}}

This is a static overflow template example.

Topic captured from the host-selected context:
`{{topic}}`

Use this when placeholder substitution is enough and full Lua generation is unnecessary.
