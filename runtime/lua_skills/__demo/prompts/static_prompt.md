Use this static prompt file as the simplest prompt template form.

It is returned as-is when the prompt definition points to a non-Lua file.

Copy this file and replace the text with your own workflow instructions.
