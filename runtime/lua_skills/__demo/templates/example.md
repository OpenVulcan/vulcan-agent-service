# Static Demo Example: {{topic}}

This is a static resource-template example.

Topic captured from the URI:
`{{topic}}`

Use this when placeholder substitution is enough and full Lua generation is unnecessary.
