{
  "name": "codeview_ast",
  "tool_name": "codeview_ast",
  "description": "[CORE INFRASTRUCTURE / INVOKE FIRST] Ultra-fast AST code structure analyzer. When taking over a project, locating definitions, or assessing refactoring impact, you MUST prioritize this tool to establish global/local context. It returns a dehydrated structural map with exact line numbers in a single call, heavily replacing traditional grep searches.",
  "lua_entry": "main.lua",
  "lua_module": "codeview_ast",
  "init_scripts": {
    "ps1": "init.ps1",
    "sh": "init.sh"
  },
  "debug": true,
  "parameters": [
    {
      "name": "path",
      "type": "string",
      "description": "Target path. Supports a single file/directory, or multiple paths separated by newlines (\\n). Required for a fresh search, but omitted when requesting a later page with cache_id.",
      "required": false
    },
    {
      "name": "recursive",
      "type": "boolean",
      "description": "Whether to scan subdirectories recursively when a directory is encountered (default: false). Keep this false if you only need shallow entry points of the current directory.",
      "required": false
    },
    {
      "name": "ignore",
      "type": "boolean",
      "description": "Enable default blacklist and scoped .gitignore rules (default: true). STRONGLY RECOMMENDED to keep enabled to prevent context explosion, unless you explicitly need to inspect build artifacts like target/dist.",
      "required": false
    },
    {
      "name": "ext",
      "type": "string",
      "description": "Extension filter, comma-separated (e.g., js,ts,rs). When scanning large directories, it is STRONGLY RECOMMENDED to explicitly specify 'ext' to avoid hitting irrelevant files and triggering the 5000-file circuit breaker limit.",
      "required": false
    },
    {
      "name": "comment",
      "type": "boolean",
      "description": "Include Doc comments of functions in the structural output (default: false). Keep false if you only need line numbers for routing; set to true if understanding complex business logic is required.",
      "required": false
    },
    {
      "name": "cache_id",
      "type": "string",
      "description": "Pagination cache identifier returned by a previous truncated search. Use this instead of path when requesting another page.",
      "required": false
    },
    {
      "name": "page",
      "type": "number",
      "description": "Page number to fetch from a cached paginated result (default: 1).",
      "required": false
    },
    {
      "name": "truncate_chars",
      "type": "number",
      "description": "Character budget per page (default: 20000). Do not override this unless the user explicitly requests a different budget or the prompt specifically requires it; the default is recommended.",
      "required": false
    },
    {
      "name": "cache_ttl_sec",
      "type": "number",
      "description": "Optional cache lifetime in seconds for paginated results. When omitted, the system default is used and values above the configured maximum are clamped automatically.",
      "required": false
    }
  ],
  "return_type": "table",
  "prompt": "[OPERATION PROTOCOL] 1. EXPLORE FIRST: ALWAYS invoke this tool before executing file reads (read_file) or text searches (grep/rg). Use the precise line numbers and AST signatures as anchors for your next steps. 2. ANTI-EXPLOSION: This tool has a strict 5000-file circuit breaker limit for matched files and allows at most 20 explicitly listed files in one call; do not mix file paths and directory paths. 3. PAGINATION: Large results may be paginated with cache_id + page. 4. BUDGET CONTROL: truncate_chars defaults to 20000 and should not be overridden unless the user explicitly asks for a different budget or the prompt requires it.",
  "resources": [
    {
      "uri": "skill://codeview_ast/guide",
      "name": "codeview_ast Guide",
      "description": "Reference guidance for using codeview_ast safely and effectively.",
      "mime_type": "text/markdown",
      "file": "resources/guide.md"
    }
  ],
  "resource_templates": [
    {
      "uri_template": "skill://codeview_ast/example/{topic}",
      "name": "codeview_ast Example",
      "description": "Parameterized codeview_ast usage example generated from a topic.",
      "mime_type": "text/markdown",
      "file": "templates/example_generator.lua"
    }
  ],
  "prompts": [
    {
      "name": "codeview_ast_first_pass",
      "description": "Prompt template that teaches an agent to start with codeview_ast.",
      "file": "prompts/first_pass.md",
      "role": "user",
      "arguments": [
        {
          "name": "target",
          "description": "The initial file or directory the agent should inspect.",
          "required": false
        },
        {
          "name": "goal",
          "description": "The coding or analysis goal that motivates the scan.",
          "required": false
        },
        {
          "name": "constraints",
          "description": "Optional operating constraints such as extension filters or shallow scanning.",
          "required": false
        }
      ]
    }
  ]
}
