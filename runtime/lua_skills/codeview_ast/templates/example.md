# codeview_ast Example: {{topic}}

Recommended call:
`codeview_ast(path="{{path}}", recursive={{recursive}}, ignore={{ignore}}, ext="{{ext}}")`

Why this shape:
{{reason}}

Next step after the AST pass:
{{next_step}}
