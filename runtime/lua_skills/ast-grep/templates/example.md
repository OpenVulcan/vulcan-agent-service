# ast-grep Example: {{topic}}

Recommended call:
`vmcp-ast(path="{{path}}", recursive={{recursive}}, ignore={{ignore}}, ext="{{ext}}")`

Why this shape:
{{reason}}

Next step after the AST pass:
{{next_step}}
