# `vulcan-ai-memory-lancedb-test`

Use this workflow to verify that the current skill has a working host-managed LanceDB binding.

What it checks:

- table creation
- vector upsert
- vector search

Use it when validating a new host, a new provider build, or a new embedding/vector path.
