# `vulcan-work-memory-sqlite-test`

Use this workflow to verify that the current skill has a working host-managed SQLite work-memory binding.

What it checks:

- database creation or reuse
- insert path
- latest-state readback

Use it when validating the work-memory provider or confirming that the skill-specific database path is available.
